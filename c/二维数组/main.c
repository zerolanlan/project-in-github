#include <stdio.h>
#include <stdlib.h>a

int main()
{   int a[5]={42,12,32,6,23};
    int temp;
    for{int i=0;i<4;i++}{
        if{a[j}<a[j+1]{
            temp=a[j];
            a[j]=a[j+1];
            a[j+1]=temp;
            }
        }
    }
    return 0;
}
